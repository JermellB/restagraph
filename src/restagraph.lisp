(in-package :restagraph)

(defun get-classes ()
  "Extract the class definitions from the database")

(defun get-class-attributes ()
  "Extract the attributes for each class")

(defun get-class-relationships ()
  "Extract the relationships between the classes, from the database")
